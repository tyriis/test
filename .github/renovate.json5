{
  enabled: true,
  // enable pre-commit, see: https://docs.renovatebot.com/modules/manager/pre-commit/
  "pre-commit": {
    enabled: true,
  },
  labels: ["dependencies"],
  packageRules: [
    // label patches
    {
      matchUpdateTypes: ["patch"],
      labels: ["patch"],
    },
    // label minor updates
    {
      matchUpdateTypes: ["minor"],
      labels: ["minor"],
    },
    // label major updates
    {
      matchUpdateTypes: ["major"],
      labels: ["major"],
    },
    // label optional dependencies
    {
      matchDepTypes: ["optionalDependencies"],
      addLabels: ["optional"],
    },
    // label peer dependencies
    {
      matchDepTypes: ["peerDependencies"],
      addLabels: ["peer"],
    },
    // label dev dependencies
    {
      matchDepTypes: ["devDependencies"],
      addLabels: ["dev"],
    },
    // group all major eslint packages in a single branch/PR
    {
      matchPackagePatterns: ["eslint"],
      groupName: "eslint packages major",
      labels: ["eslint", "major"],
      matchUpdateTypes: ["major"],
    },
    // group all minor eslint packages in a single branch/PR
    {
      matchPackagePatterns: ["eslint"],
      groupName: "eslint packages minor",
      labels: ["eslint", "minor"],
      matchUpdateTypes: ["minor"],
    },
    // group all patch eslint packages in a single branch/PR
    {
      matchPackagePatterns: ["eslint"],
      groupName: "eslint packages patch",
      labels: ["eslint", "patch"],
      matchUpdateTypes: ["patch"],
    },
    // group all storybook packages in a single branch/PR
    {
      matchPackageNames: ["storybook"],
      matchPackagePatterns: ["^@storybook/"],
      groupName: "storybook packages",
      labels: ["storybook"],
    },
    // group all react major packages in a single branch/PR
    {
      matchPackageNames: ["react"],
      matchPackagePatterns: ["^(@types/react|react|react-dom|next)$"],
      groupName: "react packages major",
      labels: ["react", "major"],
      matchUpdateTypes: ["major"],
    },
    // group all react minor packages in a single branch/PR
    {
      matchPackageNames: ["react"],
      matchPackagePatterns: ["^(@types/react|react|react-dom|next)$"],
      groupName: "react packages minor",
      labels: ["react", "minor"],
      matchUpdateTypes: ["minor"],
    },
    // group all react patch packages in a single branch/PR
    {
      matchPackageNames: ["react"],
      matchPackagePatterns: ["^(@types/react|react|react-dom|next)$"],
      groupName: "react packages patches",
      labels: ["react", "patch"],
      matchUpdateTypes: ["patch"],
    },
    // group all react packages in a single branch/PR
    {
      matchPackageNames: ["react"],
      matchPackagePatterns: ["^(@types/react|react|react-dom|next)$"],
      groupName: "react packages",
      labels: ["react"],
    },
  ],
  timezone: "Europe/Vienna",
  commitMessagePrefix: "ABC-123: [Change]",
}
